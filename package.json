{
  "name": "sprout-install",
  "version": "0.2.0",
  "description": "Diagnose and fix local install/config/PATH problems for developer tools. Not a general coding agent.",
  "author": "murderszn",
  "homepage": "https://murderszn.github.io/sprout/",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/murderszn/sprout.git"
  },
  "bugs": {
    "url": "https://github.com/murderszn/sprout/issues"
  },
  "keywords": [
    "cli",
    "install",
    "developer-tools",
    "path",
    "package-manager",
    "homebrew",
    "diagnostics",
    "pollinations",
    "windows"
  ],
  "type": "module",
  "license": "MIT",
  "bin": {
    "sprout": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc && node -e \"const fs=require('fs'),p=require('path');fs.mkdirSync(p.join('dist','knowledge','tools'),{recursive:true});for(const f of fs.readdirSync(p.join('src','knowledge','tools')).filter(x=>x.endsWith('.json')))fs.copyFileSync(p.join('src','knowledge','tools',f),p.join('dist','knowledge','tools',f))\"",
    "dev": "tsx src/cli.ts",
    "test": "node --import tsx --test test/*.test.ts",
    "prepare": "npm run build",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "chalk": "^5.4.1",
    "commander": "^13.1.0",
    "dotenv": "^16.4.7",
    "execa": "^9.5.2",
    "openai": "^4.90.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
